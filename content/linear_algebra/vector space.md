---
title: "vector space"
tags: ["linear_algebra"]
aliases: ["벡터 공간"]
---

# Vector Space ?

벡터 공간이란 어떤 벡터 집합을 의미하며, 그 집합 내 어떠한 벡터들 끼리 더하거나 서로 곱해도 집합 내 벡터가 된다.

vector space $\mathbf{R}^{n}$ 는 $n$ 개의 원소가 존재하는 모든 column vectors 벡터 $v$ 들로 이루어져 있다.

## 유한, 무한 차원 공간

어떤 vector space 이 유한한 개수의 벡터들에 의해 [[span]] 형태로 구성된다면, 이를 유한 차원 (finite-diemnsional) 이라고 말하고, 그 반대는 무한 차원 (infinite-dimensional) 이라고 말한다.

# Vector Space 특성

1. vector space $S$ 만약 $\boldsymbol{v}$ 와 $\boldsymbol{w}$ 가 vector space $S$ 에 존재한다면, 모든 조합 $c\boldsymbol{v}+d\boldsymbol{w}$ 도 반드시 $S$ 에 존재한다.
2. $S$ 에 존재하는 vectors 들은 $x$ 의 함수들이나 matrices 가 될 수 있다.

# 관련 정의들

* $n$ 개의 complex numbers 가 존재하는 vector 는 $\mathbf{C}^{n}$ 공간에 있다고 한다.

# Related

* [[subspace]]
* [[Introduction to Linear Algebra]], chapter 3
* [[column space]]

# References
